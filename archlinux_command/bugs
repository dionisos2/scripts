"Pacman is currently in use, please wait." -> rm /var/lib/pacman/db.lck

manque lib32-machin -> voir dans /etc/pacman.conf et rajouter multilib

magit-show show nothing -> se mettre à la racine du dépot git

mount /dev/sda1 /media/dd/ -o umask=000 // quand vfat inaccessible en écriture

bug wikimedia :
- Installer mise à jour en suivant les étapes décrite
- Virer un drop table dans un des fichiers sql de maintenance
- Supprimer et recréer cl_sortkey

problème avec les package dans emacs:
"package--read-archive-file: End of file during parsing"
supprimer les fichiers ~/.emacs.d/elpa/archives/le_dépot/archive-contents fonctionne, voir :
http://emacs.stackexchange.com/questions/19576/how-to-fix-this-startup-package-initialization-problem